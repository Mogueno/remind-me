import React from 'react';
import {Text, View} from 'react-native';
const Settings = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>Push notification permissions, theme, about, language</Text>
    </View>
  );
};

export default Settings;
